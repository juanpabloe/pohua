clase Principal:

  metodo vacuo principal:

     ent num_negativo1;
     flot num_negativo2;
     flot resultado;

   imprime("Dame un entero n: ");
	 num_negativo1 = lee();
	 num_negativo2 = -6.0;

	 resultado = num_negativo2 - -6;
	 imprime(resultado);

	 imprime(num_negativo1);
	 imprime(num_negativo2);

	 resultado = num_negativo2 * -6;
	 imprime(resultado);

  fin

fin

