clase Principal:

  metodo flot calcula(flot numero, flot dos):
    si?(numero == dos):
      regresa numero;
    sino
      regresa 0.0;
    fin
  fin

  metodo vacuo principal:

     flot foo;
     foo = calcula(10.0, 5.0);
      
     imprime(foo);

  fin
     
fin
